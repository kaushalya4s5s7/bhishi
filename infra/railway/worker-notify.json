{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm --filter @bhishi/db generate && pnpm --filter @bhishi/workers build"
  },
  "deploy": {
    "startCommand": "node dist/notify.js",
    "restartPolicyType": "ON_FAILURE"
  }
}
