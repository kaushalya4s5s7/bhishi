# Postgres connection string used by Prisma (migrations + client).
# Local dev via infra/docker-compose.yml:
DATABASE_URL="postgresql://bhishi:bhishi@localhost:5432/bhishi?schema=public"
# Production: override with a managed instance (Neon/Supabase), e.g.
# DATABASE_URL="postgresql://user:pass@host/db?sslmode=require"
